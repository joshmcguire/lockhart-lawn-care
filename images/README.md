Swap each .svg placeholder for a real photo: save the photo as the same base name
with .jpg (e.g. church.jpg), then update the matching <img src> in index.html from
.svg to .jpg. Keep photos under ~300KB (resize to ~1200px wide max).
work-1/2/3 are the "recent work" gallery. Add a new photo every job and rotate them.
